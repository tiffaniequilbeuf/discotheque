{% extends 'base.html.twig' %}

{% block title %}Programmation{% endblock %}

{% block body %}
    <h1>Programmation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ programmation.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ programmation.name }}</td>
            </tr>
            <tr>
                <th>DateParty</th>
                <td>{{ programmation.dateParty ? programmation.dateParty|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ programmation.description }}</td>
            </tr>
            <tr>
                <th>CreateAt</th>
                <td>{{ programmation.createAt ? programmation.createAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_programmation_index') }}">back to list</a>

    <a href="{{ path('app_programmation_edit', {'id': programmation.id}) }}">edit</a>

    {{ include('programmation/_delete_form.html.twig') }}
{% endblock %}
